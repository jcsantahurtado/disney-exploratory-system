package com.juansanta.disney.security.enums;

public enum RoleName {
    // The application has two roles:
    // admin(has all CRUD permissions) and user(see characters, movies or genres)
    ROLE_ADMIN,
    ROLE_USER
}
